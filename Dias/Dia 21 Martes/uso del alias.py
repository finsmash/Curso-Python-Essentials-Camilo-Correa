#los alias en los modulos
#permitir que el modulo se codifique mas pequeño cuando 

#es decir el modulo original ya no estara disponible, solo se hara uso del nuevo alias

from math import pi as p, sin as s

print(s(p/2))
